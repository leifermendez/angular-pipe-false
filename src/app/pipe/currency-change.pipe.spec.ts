import { CurrencyChangePipe } from './currency-change.pipe';

describe('CurrencyChangePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyChangePipe();
    expect(pipe).toBeTruthy();
  });
});
